function function20() {
    var sent1 = "Hello, "; //Sets the first variable up.
    sent1 += "World!";  //Sets the 2nd variable up and the operator(+) concatinates the variables.
    document.getElementById("paragraph1").innerHTML = sent1; // The method gets the id and sets the html element by ID.
}
