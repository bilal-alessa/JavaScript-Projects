var X = "Hello, World!", Y = " the temperature might be "; // Assigning 2 variables.
var Y = Y.fontcolor("red"); // Assigning one variable to have a font color change.
window.alert(X);    // Window alert to open as the page is interpreted.
document.write(X + Y); // Printed on screen the variables concatenated amongst each other.
document.write(35 + 60) // Both variables added together from an expression.
